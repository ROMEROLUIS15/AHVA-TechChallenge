{
  "name": "ceplan-web-client",
  "version": "1.0.0",
  "private": true,
  "description": "Interacciones de cliente (TypeScript) del portal de acceso CEPLAN.",
  "scripts": {
    "build:ts": "tsc -p tsconfig.json",
    "watch:ts": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
